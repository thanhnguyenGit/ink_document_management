#![cfg_attr(not(feature = "std"), no_std, no_main)]
#[cfg(test)]
mod tests {
    use permisson_control::permisson_control::{PermissonControl, PermissonControlRef};

    #[ink::test]
    fn default_works() {}

    #[ink::test]
    fn it_works() {}
}
